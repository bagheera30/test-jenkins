apiVersion: apps/v1
kind: Deployment
metadata:
  name: springboot
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: springboot
  template:
    metadata:
      labels:
        app: springboot
    spec:
      containers:
        - name: springboot
          image: bagheeraid/test-jenkins:v1.0.0
          imagePullPolicy: Always
          ports:
            - containerPort: 8080