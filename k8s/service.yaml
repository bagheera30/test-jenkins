apiVersion: v1
kind: Service
metadata:
  name: springboot
  namespace: default
spec:
  selector:
    app: springboot
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: NodePort